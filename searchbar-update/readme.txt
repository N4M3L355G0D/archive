webpage should be:

thumbnail of desired clipart -> url link to clipart for download -> keyword used to bring up clipart -> keyword string in database -> clipart md5sum id of clipart ( delimit the id with dashes every 4 characters )


each clipart image will need to have a png thumbnail created corresponding to the eps of that file for web page display

search box should be at the top of the results page as well
