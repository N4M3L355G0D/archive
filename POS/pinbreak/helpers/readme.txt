# access db and print total combined for customers
python readEntry.py -f BILL_DATE -v 1-22-2017 | ./total.awk 

readDups.py -- get repeat phone renewals
./pinbreak.cfg is for dev purposes only, for actual config, use ../pinbreak.cfg
