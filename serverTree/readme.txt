#a python3 script to:
scan a directory -> create an xml manifest of its contents -> zip the xml manifest and the directory up into an archive -> send to a remote server expecting a zipfile

#todos
need to add cmdline interface, add a prompt to ask if local zip copy is to be kept or deleted

#standalone modules not used but still provided for ed purposes
zipGen.py
xmlGen.py

#complete utility with all modules built in
#includes cmdline utility
serverTree.py


#best of luck,
NoGuiLinux
