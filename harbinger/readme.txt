a place for destructive tools and recipes


directory harbinger:
	harbinger.sh
		--destroy the mother server
	activator.cron
		--cron job to run harbinger.sh on a specific date
	install.sh
		--install everything
