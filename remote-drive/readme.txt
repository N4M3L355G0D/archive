copy .remote-share to your $HOME directory
open $HOME/.remote-share and fill in the config options
copy remote-drive.sh to your bin directory, and run remote-drive.sh [ --override-cfg ]
do `chmod +x` your bin copy of remote-drive.sh 

where:
 --override-cfg is optional, and allows ignorance of the $HOME/.remote-share cfg file
